/** @type {import('next').NextConfig} */
const nextConfig = {
  eslint: {
    // Remove or comment out the following line to ensure ESLint errors are not ignored in production builds
    // ignoreDuringBuilds: true,
  },
}

module.exports = nextConfig